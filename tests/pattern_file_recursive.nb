# Request a whole-module import of a nested submodule. Python has no relative
# 'import X' statement, so this must come out in absolute form.
py_recursive_stub_test.__suffix__:
    \import py_recursive_stub_test.sub.mod
